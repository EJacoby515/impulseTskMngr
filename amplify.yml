version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npx expo install
    build:
      commands:
        - npx expo export
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - $(npm root --global)/**/*