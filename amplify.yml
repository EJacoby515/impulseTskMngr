version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 18
        - npm install -g npm@latest
        - npm install
    build:
      commands:
        - npx expo export
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*