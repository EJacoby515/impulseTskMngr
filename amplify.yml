version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 16
        - npm ci
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*